#!/usr/bin/env node
const program = require("commander");
const createProject = require("../command/create");
const config = require('../templates.config');

program
  .command("new [project]")
  .description("生成一个新项目")
  .action(function (project) {
    createProject(config, project);
  });

program.parse(process.argv);
